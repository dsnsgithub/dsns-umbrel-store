version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: dsns-ytdlp_app_1
      APP_PORT: 8081

  app:
    build: .
    restart: on-failure
    stop_grace_period: 1m
    # No volumes mounted—everything is in-memory or temporary within the container